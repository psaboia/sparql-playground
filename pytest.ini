[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    -s
    --color=yes
    --durations=10
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning